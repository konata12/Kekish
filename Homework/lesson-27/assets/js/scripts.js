const names = [
    'Biba', 'Boba', 'Ne Biba', 'Ne boba', 'Bob',
    'Ne Bob', 'Maks', 'Ne Maks', 'Ragnar', 'Ne ragnar'
]

for ( let i = 0; i <= names.length; i++ ) {
    if ( names[i] === 'Ragnar' ) {
        console.log( names[i] + ' Lodbrok')
    }
}